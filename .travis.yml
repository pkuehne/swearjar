---
language: cpp
sudo: required
services:
    - docker
before_install:
    - docker build -t swearjar .
jobs:
    include:
        - stage: build and test
          script: mkdir -p build && docker run --rm -v"$PWD:/buildarea" swearjar /bin/sh -c 'cd build && cmake .. && make && make test'
