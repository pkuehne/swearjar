---
language: cpp
sudo: required
services:
    - docker
before_install:
    - docker build -t swearjar .
jobs:
    include:
        - stage: build and test
          script: mkdir -p build && ./scripts/docker.sh
